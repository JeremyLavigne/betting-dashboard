import React from 'react';

const Navbar: React.FC = (): JSX.Element => {
    console.log('navbar');

    return (
        <div>
            <p>I am a navbar</p>
        </div>
    );
};

export default Navbar;
